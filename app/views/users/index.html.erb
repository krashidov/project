<h1>All users</h1>

<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <div class = "span3">
      <%= link_to image_tag(user.photo), user  %> 
      <%= link_to user.name, user %>
      </div>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
