<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new post...", size: "2x3" %>
  </div>
  <%= f.submit "Post", class: "btn btn btn-primary" %>
<% end %>

